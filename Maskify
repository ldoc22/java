public class Maskify {
    public static String maskify(String str) {
    String x = "";
    for(int i = 0; i < str.length(); i++){
      if(i >= str.length() - 4)
        x += str.charAt(i);
      else
         x += '#';
    }
      return x;
    }
}
